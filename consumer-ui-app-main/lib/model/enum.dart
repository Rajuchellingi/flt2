enum InputType {
  email,
  password,
  text,
  confirmPassword,
  number,
  multiLine,
  checkbox,
  radio,
  date,
  file,
  select,
  phone,
}

enum DropDownFiledType { gender, memberType }

enum UserType { farmer, buyer }

enum HomeSettingType { banner, promotion }

enum HomeDisplayType { slider, fixed, carousel }

enum DropFields {
  state,
  district,
  unit,
  accessMandi,
  organic,
  village,
  irrigation
}
